import './bootstrap';
import { initContactForm } from './contact-form';

// Inicializa o formulário de contato quando o DOM estiver pronto
document.addEventListener('DOMContentLoaded', function() {
    initContactForm();
});

